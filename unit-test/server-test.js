var chai = require('chai');
var expect = chai.expect;

var Server = require('./../server');

describe('Main server',function () {
    it('should have endpoint /processRequestToProvideCoordinate',function () {

    })
});